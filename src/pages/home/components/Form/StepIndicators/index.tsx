import { FormStep } from '../../../hooks/useFormStep/types'
import styles from './styles.module.css'

interface Props {
  activeStep: FormStep
}

const StepIndicators = ({ activeStepIndex, steps }: Props) => {
  return <aside></aside>
}

export default StepIndicators
